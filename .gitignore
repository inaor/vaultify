dist/
*.exe
*.exe~
*.bak
.env
op.env
*.pem
*.key
sessions/
.cursor/
__debug_bin*

# Accidental local macOS binary drops (ship via GitHub Releases, not git)
vaultify-macos-arm
vaultify-macos-intel

# Local secret material (do not commit)
.dev-keys/
*.priv
license.jwt

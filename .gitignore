dist/
*.exe
*.exe~
*.bak
.env
op.env
*.pem
*.key
sessions/
.cursor/
__debug_bin*

# License signing material — public keys live in source, private keys
# never do. Real production private keys go straight into the
# Cloudflare Worker secret store via `wrangler secret put`.
.dev-keys/
*.priv
license.jwt

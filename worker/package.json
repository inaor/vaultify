{
  "name": "vaultify-license-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that signs Vaultify Pro license JWTs (Ed25519) on billing webhooks. The Worker is the licensing trust root; vaultify.exe verifies offline against the embedded public key.",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/ed25519": "^2.1.0",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.114.17"
  }
}
